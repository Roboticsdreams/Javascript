module.exports = {

    area: function (r){
        let result =(Math.PI * r * r);
        return result;
    },

    perimeter : function(r) {
        let result = (2 * Math.PI * r);
        return result;
    },
};